{
  "name": "wa-notif-papua",
  "version": "1.0.0",
  "description": "WhatsApp Notification System for PLN UIW Papua & Papua Barat",
  "main": "dist/app.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/app.js",
    "dev": "ts-node-dev --respawn --transpile-only src/app.ts",
    "typecheck": "tsc --noEmit",
    "pm2:start": "pm2 start ecosystem.config.js",
    "pm2:stop": "pm2 stop wa-notif-papua",
    "pm2:restart": "pm2 restart wa-notif-papua",
    "pm2:reload": "pm2 reload wa-notif-papua",
    "pm2:logs": "pm2 logs wa-notif-papua",
    "pm2:status": "pm2 status",
    "deploy": "npm run build && npm run pm2:start",
    "logs:rotate": "pm2 install pm2-logrotate && pm2 set pm2-logrotate:max_size 10M && pm2 set pm2-logrotate:retain 7 && pm2 set pm2-logrotate:compress true && pm2 set pm2-logrotate:rotateInterval \"0 0 * * *\""
  },
  "dependencies": {
    "axios": "^1.7.2",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "mysql2": "^3.9.8",
    "node-cron": "^3.0.3"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^20.14.2",
    "@types/node-cron": "^3.0.11",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.4.5"
  }
}
